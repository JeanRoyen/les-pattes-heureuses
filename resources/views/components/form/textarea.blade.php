<div>
    <label for="{{ $name }}" class="block">Message*</label>
    <textarea name="{{ $name }}" id="{{ $name }}" cols="30" rows="3" {{ $attributes }} placeholder="{{ $placeholder }}" class="border-input-grey border-1 rounded-button pl-2 w-full"></textarea>
</div>
