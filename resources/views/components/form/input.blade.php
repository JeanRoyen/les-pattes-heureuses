<div class="grow">
    <label for="{{ $name }}" class="block">{{ $title }}</label>
    <input name="{{ $name }}" type="{{ $type }}" id="{{ $name }}" placeholder="{{ $placeholder }}" class="border-input-grey border-1 rounded-button pl-2 w-full py-1 focus:border-background-green focus:outline-none">
</div>
